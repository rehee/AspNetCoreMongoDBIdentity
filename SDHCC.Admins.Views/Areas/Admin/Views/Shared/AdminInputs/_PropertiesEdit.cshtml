@model SDHCC.DB.Content.IPassModel
@{
  var index = 0;
}
@foreach (var property in Model.Properties)
{
  await Html.RenderPartialAsync("AdminInputs/_AdminInput", new ContentPropertyIndex(property, index));
  index++;
}


